# 삼국지 영걸전 리메이크 — 상세 스펙 문서 (v1.1)

> **상위 문서**: `yeonggeoljeon-remake-design.md` (전체 설계 v1.1)
> **용도**: 클로드 코드 구현 시 각 시스템의 세부 규칙·인터페이스·수치 기준. 상위 문서와 충돌 시 본 문서가 우선한다.
> **표기**: `[검증]` = 원작 수치가 불확실해 플레이 대조로 확인이 필요한 항목. 구현은 되 값은 `combat-config.json`에서 조정한다.

---

## 목차

1. 전투 규칙 상세
2. 세이브 시스템 상세 (턴 중간 세이브 포함)
3. 이벤트 스크립트 DSL 명세
4. 캠페인·분기 상세 (플래그 설계 포함)
5. 적 AI 상세
6. 원작 맵 재현 파이프라인
7. 일기토 상세
8. UI/입력 상세
9. 데이터 스키마 전문 (zod 기준)
10. 테스트 계획
11. 장수 정보 에디터
12. 빌드 자동화 (윈도우·맥)

---

## 1. 전투 규칙 상세

### 1.1 턴 구조와 행동 경제

- 턴 순서: `플레이어 페이즈 → 적 페이즈 → 턴 카운터 +1`. 제3세력이 있는 스테이지는 `플레이어 → 제3세력 → 적` 순. `[검증]`
- 각 부대는 페이즈당 **이동 1회 + 행동 1회**. 순서는 "이동 → 행동" 고정(행동 후 이동 불가, 원작 동일). 이동만 하고 대기 가능, 이동 없이 행동 가능.
- 이동 확정 전에는 취소 가능. **행동(공격/책략/아이템) 확정 후에는 되돌리기 불가.**
- 반격 없음. 병과 플래그 `counterAttack` 보유 시에만 피격 직후 1회 반격(사거리 내일 때). `[검증]` 대상 병과 확인 필요.
- 턴 시작 처리 순서: ① 자동 저장 → ② 거점 회복(§1.7) → ③ 자연 회복 아이템 → ④ 상태이상 판정(혼란 해제 체크) → ⑤ 턴 이벤트 트리거.

### 1.2 이동

- 이동 범위는 지형별 코스트 합이 이동력 이하인 타일의 집합(다익스트라/BFS).
- 지형 코스트 기본표 (`terrain.json` 초기값, `[검증]`):

| 지형 | 보병 | 기병 | 궁병 | 수송/군악 | 주술사 | 특수(적병) |
|---|---|---|---|---|---|---|
| 평지 | 1 | 1 | 1 | 1 | 1 | 1 |
| 숲 | 1 | 진입불가 | 1 | 2 | 1 | 1 |
| 황무지 | 1 | 1 | 1 | 2 | 1 | 1 |
| 산 | 진입불가 | 진입불가 | 진입불가 | 진입불가 | 진입불가 | 1 |
| 강 | 진입불가 | 진입불가 | 진입불가 | 진입불가 | 진입불가 | 진입불가 |
| 다리/평지도로 | 1 | 1 | 1 | 1 | 1 | 1 |
| 마을/성문/성내 | 1 | 1 | 1 | 1 | 1 | 1 |

- 적 유닛이 있는 타일은 통과 불가(아군 타일은 통과 가능, 정지 불가). ZOC(지배 영역) 규칙은 없음(원작 동일). `[검증]`
- 수송대·군악대의 "이동력 반감"은 코스트 2배로 구현(위 표의 2).

### 1.3 공격

- 공격 가능 방향: 병과 데이터 `attackRange.directions`(4 또는 8). 원작 기준 단병 4방향, 장병 이상 8방향(대각 포함), 기병 전 단계 4방향. `[검증]`
- 사거리: 근접 병과 1. 궁병 1~N(궁병 2, 연노병 3, 발석차 4 초기값 `[검증]`). 원거리 병과의 최소 사거리(인접 공격 가능 여부)도 데이터로 정의.
- 데미지 계산(의사코드):

```
function physicalDamage(atk: Unit, def: Unit, ctx): number {
  const A = (atk.level + 10) * (atk.morale + atk.war + cls(atk).attackMod) * itemAtkMul(atk);
  const D = (def.level + 10) * (def.morale + def.ldr + cls(def).defenseMod) * itemDefMul(def)
            * affinityMul(cls(atk).family, cls(def).family)   // 유리 0.75, 불리 1.25, 동등 1.0 (방어측에 곱함)
            * (1 + terrainDefBonus(def.pos));
  const raw = CFG.baseDamage * A / D;                          // CFG.baseDamage 초기값 [검증: 실측 튜닝]
  return clamp(Math.round(raw * rng.range(0.9, 1.1)), CFG.minDamage, def.hp);
}
```

- 격파(병력 0): 공격측에 격파 보너스 경험치. 적장 소지 아이템 드롭(스테이지 데이터 정의 시).
- **아군 부대 괴멸 시 해당 장수는 전사하여 캠페인에서 영구 이탈**(원작 재현). `[검증]` 예외 장수(스토리 필수 인물) 목록은 `officers.json`의 `essential: true`로 관리 — 필수 장수 괴멸은 게임 오버.

### 1.4 사기와 혼란

- 사기 범위 0~100. 초기값은 스테이지/이벤트로 정의(기본 100).
- 감소: 피격 시 소량(`CFG.moraleLossOnHit`, 초기 2), 사기저하 책략(견제 -20 / 도발 -30 / 위압 -40), 아군 장수 전사 시 전체 -N `[검증]`.
- 회복: 군악대 책략(격려/응원/고무 등), 거점 위 턴 회복, 아이템(칙명서·옥새), 일기토 승리 시 전체 +N `[검증]`.
- 혼란: 사기가 `CFG.confusionThreshold`(초기 30) 미만일 때 턴 시작마다 확률 판정. 혼란 상태 부대는 그 턴 조작 불가(제자리 대기 또는 랜덤 인접 이동, `CFG.confusionBehavior`). 사기가 임계값 이상으로 회복되면 즉시 해제.
- 사기는 실효 공격/방어 계산식에 직접 더해지는 항이므로(§1.3) 낮은 사기는 그 자체로 전투력 저하를 낳는다.

### 1.5 책략 실행 규칙

- 책략치(MP)는 지력·레벨 기반 상한(`mpMax = f(int, level)`, 초기 `int/4 + level` `[검증]`). 자연 회복은 거점 위에서만. 군악대 인접 효과로 회복 `[검증]`.
- 명중률: `hit = clamp(50 + (casterInt - targetInt), CFG.tacticHitMin=10, CFG.tacticHitMax=100)` 초기식 `[검증]`. 회복계는 항상 성공.
- 데미지: `tacticDamage = base × (1 + (casterInt - targetInt)/200) × terrainWeatherMul × rng(0.9~1.1)` 초기식 `[검증]`.
- 지형·날씨 게이트(§상위 문서 2.4 요약을 규칙화):
  - 화계: 우천 시 사용 불가. 대상 타일이 숲이면 ×1.25.
  - 수계: 시전자/대상 조건 — 평지·다리 계열 타일만. 우천 시 ×1.25.
  - 지계: 대상 타일이 산·황무지일 때만. 날씨 무관.
  - 거점 타일(마을/성문/성내) 위 부대에게 공격 책략 무효(사기저하·혼란 포함 여부 `[검증]`).
- "대(大)" 계열: 대상 타일 + 상하좌우 십자 5타일의 부대에 일괄 적용(아군 오폭 포함 `[검증]`).
- 사용 가능 병과: `tactics` 카테고리를 병과 데이터가 참조(보병계=화계+병력회복 일부, 궁병계=수계, 기병=공격 책략 없음, 수송대=병력회복, 군악대=사기회복, 주술사=전 카테고리). 콘솔판 풍계는 v1에서는 제외하되 데이터 구조는 카테고리 추가만으로 지원 가능하게 한다.

### 1.6 경험치·레벨·승급

- 경험치 이벤트: 공격 성공(데미지 비례, `exp = clamp(dmg/CFG.expDivisor, 1, 40)` `[검증]`), 격파 보너스(+40~50 `[검증]`), 책략 성공, 회복 행동(회복량 비례), 소모 아이템 사용, 일기토 승리(+100 = 즉시 레벨업).
- 레벨업: 누적 100마다 레벨 +1, 최대 99. 레벨업 시: 병력 상한 +`growth.hpPerLevel`(병력도 동량 회복 `[검증]`), 책략치 상한 재계산.
- 적 격파 경험치는 막타 부대가 독식(원작 재현). 낮은 레벨 부대 육성 편의 옵션(경험치 공유)은 `game-config.json` 토글로 제공(기본 OFF = 원작).
- 승급: `upgradeLevel` 도달 + 승급 아이템 사용(단병→장병: 장창 Lv15+, 장병→전차대: 보병차 Lv30+, 중기병→친위대: 근위개 Lv30+ 등). 승급 시 병과가 교체되고 스프라이트·능력 보정이 데이터 기준으로 갱신. 병과 전환서(검술/궁술/마술지침서)는 계열 자체를 바꾼다(레벨 유지 `[검증]`).
- **장수별 사용 제한 없음(설계 결정)**: 병과 전환서·승급 아이템의 사용 조건은 병과·레벨 조건만 존재하며, 특정 장수를 배제하는 제한(원작의 "유비 병과 변경 불가" 등)은 **채택하지 않는다**. 유비를 포함한 모든 아군 장수가 조건 충족 시 사용 가능하다. 아이템 스키마의 `forbiddenFor` 필드는 개조 확장용으로만 남겨두고, M4 데이터 입력 시 어떤 병과 변경/승급 아이템에도 값을 넣지 않는다(validate에서 경고 항목으로 체크).

### 1.7 지형 효과·거점

- 방어 보정(초기값 `[검증]`): 평지 0%, 숲 +10%, 황무지 +5%, 마을 +15%, 성문/성벽 +20%, 성내 +25%.
- 거점(마을/성문/성내) 위 턴 시작 회복: 병력 +10%, 사기 +5, 책략치 +N `[검증]`.
- 군량고/보물고: 위에서 "조사" 행동(또는 진입 시 자동 `[검증]`)으로 아이템/자금 획득. 1회성. 스테이지 데이터에 좌표·내용 정의.
- 날씨: 스테이지 정의(`fixed: clear|rain` 또는 `dynamic: 확률, 지속턴`). 비는 화계 금지·수계 강화 외에 이동 영향 없음 `[검증]`.

---

## 2. 세이브 시스템 상세 (턴 중간 세이브)

### 2.1 요구사항 (상위 문서 §3.6)

원작 영걸전은 월드맵에서만 저장 가능했다. 본 리메이크는 **조조전과 유사하게 전투 중 플레이어 턴 임의 시점에 저장·로드**할 수 있어야 한다.

### 2.2 저장 가능 조건 (상태 기계 기준)

| 게임 상태 | 수동 저장 | 비고 |
|---|---|---|
| 월드맵/중간 메뉴 | 가능 | `type: worldmap` |
| 전투 — 플레이어 페이즈, 입력 대기 중 | **가능** | `type: battle` |
| 전투 — 유닛 이동/공격 애니메이션 재생 중 | 불가 | 연출 종료 후 가능 |
| 전투 — 이벤트/대화/일기토 진행 중 | 불가 | 이벤트 종료 후 가능 |
| 적/제3세력 페이즈 | 불가 | 자동 저장만 존재 |

구현 규칙: 저장은 항상 "커맨드 적용이 완결된 상태"에서만 일어난다. `applyCommand()` 실행 중간에는 저장 지점이 없으므로, BattleState는 언제나 직렬화 가능한 일관 상태다.

### 2.3 세이브 파일 포맷

```jsonc
{
  "version": 1,                      // 세이브 포맷 버전 (마이그레이션 기준)
  "dataHash": "sha256:…",            // data/ 폴더 전체 해시 — 개조 데이터 식별
  "type": "battle",                  // worldmap | battle | auto
  "savedAt": "2026-08-16T12:00:00Z",
  "label": "제2장 서주 전투 - 4턴",   // UI 표시용 자동 생성 라벨
  "campaign": { /* CampaignState 전체 */ },
  "battle": {                        // type=battle/auto(전투 중)일 때만
    "stageId": "stage-012",
    "turn": 4, "phase": "player",
    "weather": "rain",
    "units": [ { "officerId": "guan_yu", "hp": 4200, "morale": 86, "mp": 12,
                 "pos": [14, 7], "acted": false, "status": "normal", "items": ["…"] } ],
    "firedEvents": ["ev-012-03"],    // 발동 완료 이벤트 ID (재발동 방지)
    "treasuresTaken": [[10,3]],
    "rng": { "algo": "xoshiro128ss", "state": [1234, 5678, 91011, 1213] }
  }
}
```

### 2.4 RNG 정책

- 전역 RNG는 `xoshiro128**`(구현 간단, 직렬화 가능한 128bit 상태). 전투 시작 시 시드 = 캠페인 시드 + 스테이지 ID + 진입 횟수.
- 로드 시 동작은 설정 `rngOnLoad`를 따른다.
  - `reseed`(기본): 로드 시 새 시드. 같은 행동도 결과가 달라질 수 있음 → 세이브/로드 리세마라 가능(조조전식 플레이 감각).
  - `restore`: 저장된 상태 복원. 같은 행동 → 같은 결과(엄격 모드).
- 테스트에서는 항상 `restore` + 고정 시드.

### 2.5 저장소와 슬롯

- 저장소 인터페이스: `SaveStore { list(); read(slot); write(slot, data); delete(slot) }`. 구현체 2종 — `LocalStorageStore`(개발), `TauriFsStore`(패키징 후, Tauri `appDataDir()/saves/*.json` — 윈도우 `%APPDATA%\ygj-remake\`, 맥 `~/Library/Application Support/ygj-remake/`로 플랫폼별 자동 해석).
- 슬롯: 수동 8개 + 자동 3개(롤링: 턴 시작마다 auto-1→2→3 순환) + 전투 개시 직전 자동 1개(`pre-battle`).
- 로드 UI에는 라벨·저장 시각·스테이지·턴·스크린샷 썸네일(캔버스 캡처, 선택 구현)을 표시.

### 2.6 호환성·무결성

- `version` 불일치: 마이그레이션 함수 체인으로 승급(불가 시 명확한 에러 메시지).
- `dataHash` 불일치(세이브 이후 데이터 개조): 로드는 허용하되 경고 표시. 참조 무결성 검사(존재하지 않는 병과/아이템 ID)를 로드 시 수행하고, 삭제된 ID는 안전한 기본값으로 폴백(병과→계열 1티어, 아이템→제거)한 뒤 로그로 알린다. **개조를 전제로 한 프로젝트이므로 이 폴백 규칙이 중요하다.**
- 저장은 원자적으로: 임시 파일에 쓰고 rename(전원 차단 시 세이브 파괴 방지).

---

## 3. 이벤트 스크립트 DSL 명세

이벤트는 JSON으로 기술한다. 인터프리터는 `core/campaign/eventRunner.ts`.

### 3.1 구조

```jsonc
{ "id": "ev-012-03",
  "scope": "battle",                 // battle | camp (전투 밖)
  "once": true,                      // 1회성 여부
  "trigger": { … },                  // 발동 조건 (§3.2)
  "condition": { … },                // 추가 조건 (플래그 등, 선택)
  "actions": [ … ] }                 // 순차 실행 (§3.3)
```

### 3.2 트리거 종류

| 트리거 | 파라미터 | 예 |
|---|---|---|
| `battleStart` | — | 전투 개시 연출 |
| `turnStart` / `turnEnd` | `turn`(정확히) 또는 `fromTurn`(이상) | 5턴 증원 |
| `unitAttacks` | `attacker`, `target` | 관우가 화웅 공격 → 일기토 |
| `unitAdjacent` | `unit`, `target` | 인접 시 대화 |
| `unitHpBelow` | `unit`, `ratio` | 적장 도주 |
| `unitDestroyed` | `unit`, `by`(선택) | 격파 대사 |
| `unitReaches` | `unit`, `area: [x1,y1,x2,y2]` | 탈출점 도달 |
| `unitsRemaining` | `side`, `count` | 적 3부대 이하 |
| `stageEnter` / `stageClear` | — | 캠프 이벤트 |

### 3.3 액션 종류

| 액션 | 파라미터 | 비고 |
|---|---|---|
| `dialogue` | `lines: [{speaker, portrait, text}]` | 대화창 |
| `choice` | `prompt`, `options: [{text, actions}]` | 선택지 — 중첩 액션 |
| `duel` | `a`, `b`, `outcome: "aWins"|"bWins"|"judge"`, `onAWin/onBWin` | §7 |
| `spawnUnits` | `units: [배치 정의]`, `side` | 증원 |
| `removeUnit` / `moveUnit` | `unit`, `pos` | 도주·강제이동 연출 |
| `setFlag` / `clearFlag` | `flag` | 캠페인 플래그 |
| `giveItem` / `giveGold` / `giveExp` | 대상, 양 | 보상 |
| `joinOfficer` / `leaveOfficer` | `officerId` | 가입/이탈 |
| `setWeather` | `weather` | 이벤트성 날씨 변화 |
| `changeMorale` | `side|unit`, `delta` | 사기 변동 연출 |
| `endBattle` | `result: victory|defeat` | 조건 외 종료 |
| `gameOver` | `reason` | 선택지 즉사(원작 재현) |
| `playBgm` / `playSfx` / `wait` | — | 연출 |

### 3.4 실행 규칙

- 트리거 평가 시점: 각 커맨드 적용 직후 + 턴 경계. 동시 발동 시 스테이지 데이터 배열 순서대로.
- `choice` 실행 중에는 저장 불가(§2.2). `once` 이벤트는 발동 즉시 `firedEvents`에 기록.
- 액션 실행은 코어에서 상태 변경을 즉시 적용하고, 렌더러가 `BattleEvent` 큐를 순차 연출한다(스킵 버튼 지원).

---

## 4. 캠페인·분기 상세

### 4.1 플래그 설계

분기는 전부 불리언 플래그 + 스테이지 결과로 판정한다. 대표 플래그(조사 기반, M4에서 전수 확정):

| 플래그 | 세팅 시점 | 영향 |
|---|---|---|
| `route.xindu` / `route.guangchuan` | 신도/광천 선택 | 가입 무장 상이(번궁 vs 한영·곽적), 경유 전투 상이 |
| `guanyu.alive` | 맥성 전투 결과(8턴 내 적 전멸 또는 관우 탈출 성공 시 생존) | 최대 분기점 — 후반 시나리오·엔딩 좌우 |
| `wu.alliance` | 오나라 화친/동맹 선택 | 이릉 루트 여부 |
| `zhangfei.alive` | 관우 사망+항전 루트에서 암살 이벤트 | 후반 전력 |
| `route.fictional` | 가상 루트 진입 판정 | 4장 이후 전투 목록 교체 |

### 4.2 게임 오버 선택지 (원작 재현)

원작에는 선택을 잘못 고르면 즉시 게임 오버가 되는 선택지가 있다(예: 서주에서 옥대 관련 답변, 관도에서 신분 관련 답변). 이는 `choice` 액션 내 `gameOver` 액션으로 구현한다. **턴 중간 세이브와 별개로, `choice` 직전 자동 저장(§2.5의 auto 슬롯)이 있어 원작처럼 가혹하지 않게 재도전할 수 있다.** 원작의 긴장감을 원하면 설정에서 choice 직전 자동 저장을 끌 수 있다.

### 4.3 엔딩 판정

- 정사 계열 엔딩: 오와 동맹/화목 또는 이릉 승리 중 하나 충족.
- 배드 엔딩(도원종언): 이릉 패배.
- 가상 루트 엔딩: `route.fictional` 진입 후 최종전 승리.
- 콘솔판의 에필로그 세부 분기(턴 수·레벨·소지금 조건)는 v1 범위 외. 판정 함수만 확장 가능하게 남겨둔다.

### 4.4 캠페인 상태

```ts
interface CampaignState {
  seed: number;
  currentStageId: string;
  gold: number;
  officers: Record<string, { level: number; exp: number; classId: string;
                             hp: number; items: string[]; alive: boolean; joined: boolean }>;
  convoy: string[];              // 공용 창고(편의 기능, 토글 가능) [원작에 없음: 기본 OFF]
  flags: Set<string>;
  stageResults: Record<string, "win"|"loss"|"skipped">;
  playTimeSec: number;
}
```

---

## 5. 적 AI 상세

### 5.1 프로필과 상태

스테이지의 각 적 부대는 `ai` 프로필과 선택적 `aiParams`를 갖는다.

| 프로필 | 행동 |
|---|---|
| `aggressive` | 매 턴 최적 타깃에게 접근/공격 |
| `defensive` | 아군이 `alertRange`(기본 6) 내로 들어오기 전까지 대기, 이후 aggressive 전환(영구) |
| `guard` | 지정 좌표/거점을 벗어나지 않고 사거리 내만 공격 |
| `support` | 아군(적측) 회복·사기 관리 우선, 위험 시 후퇴 |
| `flee` | 지정 탈출점으로 이동(이벤트 연계) |

### 5.2 행동 결정 (aggressive 기준 의사코드)

```
후보 = 이동 가능 타일 × (공격 가능 타깃 ∪ 책략 가능 타깃 ∪ {없음})
score(후보) = 예상데미지 × W1
            + 격파가능보너스 × W2          // 예상데미지 ≥ 대상 잔여 병력
            + 대상위협도 × W3               // 대상의 공격력·회복능력 평가
            - 받을반격위험 × W4             // 반격 없음이므로 인접 적 수·다음턴 피격 예상으로 대체
            + 지형방어보정 × W5
            + 접근도 × W6                   // 공격 불가 시 최근접 접근 경로
```

- 가중치 `W1~W6`은 `combat-config.json`의 AI 섹션. 난이도 프리셋(원작/쉬움/어려움)은 가중치+명중 보정 세트로 구현.
- 책략 사용 판단: 사용 가능 책략 중 기대 데미지(명중률×피해)가 통상 공격을 넘고 책략치가 충분할 때. 회복형 AI는 아군 병력 비율 임계값으로 판단.
- 성능: 부대당 후보 평가는 이동 범위(≤~120타일)×타깃(≤10) 수준이므로 전탐색으로 충분. 1페이즈 전체 100ms 이내 목표.

### 5.3 원작 감각 재현 노트

원작 AI는 "성문 앞 대기", "특정 턴까지 부동" 같은 스크립트성 행동이 많다. 이는 AI 프로필 + 이벤트(`fromTurn`에 프로필 교체 액션)로 재현한다. AI 프로필 변경 액션 `setAiProfile(unit, profile)`을 §3.3 액션 목록에 추가한다.

---

## 6. 원작 맵 재현 파이프라인 (간략화 금지 방침)

### 6.1 원칙

- 맵의 **논리 구조(가로세로 크기, 각 타일의 지형 종류, 유닛 초기 배치, 보물·군량고 좌표, 증원 등장 좌표)는 원작과 동일**하게 입력한다.
- 시각 표현(타일 그래픽)은 취향에 따라 바꾸되, 지형 종류의 좌표 매핑은 바꾸지 않는다.
- 근거 자료 확보가 안 된 타일만 `unverified` 마킹을 허용하고, 플레이 영상으로 재검증해 마킹을 소거한다. M4 완료 기준 = 전 스테이지 `unverified` 0.

### 6.2 맵 트레이싱 도구 (M3.5 산출물)

인브라우저 도구(`/tools/map-editor`)로 구현. 기능 요구사항:

1. **참조 이미지 오버레이**: 원작 스크린샷(DOSBox 캡처)을 불러와 반투명도·배율·오프셋 조절로 격자에 정렬. 여러 장(맵이 스크롤되는 경우) 이어붙이기 지원.
2. **타일 팔레트 페인팅**: 지형 선택 후 클릭/드래그로 칠하기. 채우기(flood fill), 사각 영역 지정.
3. **유닛 배치 모드**: 적/아군 배치 좌표와 소속 장수·병과·레벨·AI 프로필 입력.
4. **오브젝트 모드**: 보물고/군량고/탈출점/증원 등장점 좌표와 내용 입력.
5. **대조 토글**: 참조 이미지 ↔ 렌더링 결과를 단축키로 전환하며 눈으로 검수. 불일치 의심 타일에 `unverified` 마킹.
6. **입출력**: `stages/*.json`을 직접 읽고 쓴다(별도 변환 단계 없음). Tiled TMX 가져오기는 선택 구현.

### 6.3 스테이지 검수 체크리스트 (스테이지당 1회)

- [ ] 맵 크기(W×H)가 원작과 일치
- [ ] 지형 페인팅 완료, `unverified` 타일 0
- [ ] 아군 배치 존/최대 출진 수 일치
- [ ] 적 부대: 장수·병과·레벨·좌표·AI 일치 (공략 자료 대조)
- [ ] 보물고/군량고 좌표와 내용물 일치
- [ ] 증원: 등장 턴/좌표/구성 일치
- [ ] 승리/패배 조건 일치
- [ ] 전투 이벤트(일기토, 대사, 도주 조건) 스크립트 입력 완료
- [ ] 통플레이 1회 — 원작 공략 순서로 진행 시 위화감 없음

### 6.4 자료 수집 절차

1. DOSBox에서 원작 실행(사용자 보유분) → 스테이지별 전장 전체 스크린샷 세트 캡처(개인 작업용 참고 자료).
2. 유튜브 공략 영상·공략글로 증원/이벤트 타이밍 보강.
3. 캡처가 어려운 경우 공략 카페의 맵 이미지를 참고.
4. 수집물은 `reference/`(git 제외 폴더)에 스테이지별로 정리 — 개인 작업 자료로만 사용, 리포지토리/결과물에 포함하지 않음.

---

## 7. 일기토 상세

### 7.1 발동

- 이벤트 트리거로만 발동(원작 재현): 특정 장수 조합이 `unitAttacks` 또는 `unitAdjacent` 조건을 만족할 때.
- 자유 일기토(임의 장수 간 무력 판정)는 v1 범위 외. `duel` 액션의 `outcome: "judge"` 모드로 기반만 마련.

### 7.2 진행과 결과

- 연출: 컷인 배경 + 양측 초상/스프라이트 + 대사 교환 + 합(合) 애니메이션 반복 → 결과.
- `outcome` 고정형: 스크립트 지정 승패. 판정형(`judge`): `승률 = clamp(50 + (무력차 × 2), 5, 95)` `[검증]`, RNG 소비 1회.
- 승리 효과: 패자 부대 즉시 괴멸(퇴각 연출) 또는 퇴각(스크립트 지정), 승자 경험치 +100, 아군 전체 사기 +N. 패배 효과: 반대로 적용. 스크립트로 개별 오버라이드 가능.
- 일기토 중 저장 불가(§2.2). 일기토 직전 상태는 auto 슬롯이 커버.

---

## 8. UI/입력 상세

### 8.1 입력 매핑

| 조작 | 키보드 | 마우스 |
|---|---|---|
| 커서 이동 | 방향키 | 호버 |
| 결정 | Z / Enter | 좌클릭 |
| 취소 | X / ESC | 우클릭 |
| 유닛 정보 | C | 유닛 위 휠클릭/더블클릭 |
| 시스템 메뉴(세이브 포함) | ESC(빈 타일) | 빈 타일 우클릭 |
| 턴 종료 | E | 시스템 메뉴 내 버튼 |
| 다음 미행동 부대 | Tab | — |
| 연출 스킵 | Space 홀드 | — |
| 카메라 | WASD / 엣지 스크롤 | 드래그 |

### 8.2 전투 HUD 구성

- 좌상단: 턴 수·페이즈·날씨. 우상단: 미니맵(선택 구현).
- 유닛 호버 시: 간이 패널(이름, 병과 아이콘, 병력바, 사기). 지형 패널(지형명, 방어 보정, 이동 코스트).
- 공격 대상 지정 시: 예상 데미지·명중률·상성 유불리 아이콘(▲▼) 표시. **상성 표시는 `affinity.json`을 읽어 렌더링**(병종 개조 시 자동 갱신).
- 데미지 숫자 팝업, 병력바 애니메이션, 격파 시 퇴각 연출.

### 8.3 상태창(부대 상세)

탭 구성: ① 능력(무/지/통, 실효 공격/방어 — 계산 내역 툴팁으로 분해 표시) ② 책략(사용 가능 목록·소모·사거리) ③ 소지품(8칸, 장비 효과 표시) ④ 병과(현재/승급 트리 — `classes.json`의 `upgradesTo` 체인을 그려줌).

---

## 9. 데이터 스키마 전문 (zod)

구현 시 `src/core/data/schemas.ts`에 아래를 그대로 옮긴다(발췌 — 전체는 코드가 원본).

```ts
const ClassSchema = z.object({
  id: z.string(), name: z.string(),
  family: z.enum(["infantry","cavalry","archer","transport","band","sorcerer","special"]),
  tier: z.number().int().min(1).max(3),
  upgradesTo: z.string().nullable(), upgradeLevel: z.number().int().nullable(),
  attackMod: z.number(), defenseMod: z.number(),
  movement: z.number().int().positive(),
  attackRange: z.object({ min: z.number().int(), max: z.number().int(),
                          directions: z.union([z.literal(4), z.literal(8)]) }),
  movementRules: z.object({ forbidden: z.array(z.string()), halved: z.array(z.string()) }),
  tactics: z.array(z.string()),          // 카테고리 or 개별 ID
  flags: z.array(z.enum(["counterAttack","mountainMove","noTerrainPenalty"])),
  sprite: z.string(),
});

const OfficerSchema = z.object({
  id: z.string(), name: z.string(),
  war: z.number().int().min(1).max(100), int: z.number().int().min(1).max(100),
  ldr: z.number().int().min(1).max(100),
  classId: z.string(), essential: z.boolean().default(false),
  joinStage: z.string().nullable(), portrait: z.string(),
  duelProfile: z.object({ strength: z.number(), special: z.boolean() }).optional(),
  growth: z.object({ hpPerLevel: z.number(), mpCurve: z.string() }),
});

const StageSchema = z.object({
  id: z.string(), name: z.string(),
  map: z.object({ width: z.number().int(), height: z.number().int(),
                  tiles: z.array(z.array(z.string())) }),   // terrain.json의 ID 행렬
  weather: z.union([z.enum(["clear","rain"]),
                    z.object({ dynamic: z.object({ rainChance: z.number() }) })]),
  deployment: z.object({ maxUnits: z.number().int(), zone: z.array(z.tuple([z.number(), z.number()])) }),
  enemies: z.array(EnemyUnitSchema),
  allies: z.array(EnemyUnitSchema).optional(),               // 제3세력/우군
  victory: VictoryCondSchema, defeat: DefeatCondSchema,
  treasures: z.array(z.object({ pos: z.tuple([z.number(), z.number()]),
                                itemId: z.string().optional(), gold: z.number().optional(),
                                unverified: z.boolean().default(false) })),
  events: z.array(EventSchema),
  shopBefore: z.array(z.string()).optional(),
  next: z.union([z.string(), BranchSchema]),
  mapVerification: z.object({ status: z.enum(["draft","traced","verified"]),
                              unverifiedTiles: z.number().int() }),  // §6.3 체크리스트 연동
});
```

참조 무결성 검사(`npm run validate`) 항목: 모든 `classId`/`itemId`/`officerId`/`stageId`/`sprite`/`portrait` 참조 존재 여부, affinity 행렬의 family 전수 커버, 각 병과의 4대 애니메이션(idle/move/attack/damaged) 정의 여부, 캠페인 그래프 도달 불가능 노드·순환 검출, `unverified` 타일 수 리포트.

---

## 10. 테스트 계획

### 10.1 단위 테스트 (Vitest, `core/` 대상)

- 데미지 공식: 상성 3×3 조합별 배수 검증, 지형 보정, 아이템 보정, 경계값(최소 데미지, 병력 초과).
- 이동 범위: 지형 코스트·진입 불가·유닛 차단 케이스, 이동력 반감 병과.
- 책략: 지형/날씨 게이트 전 조합, 십자 범위 대상 산출, 명중률 경계.
- 경험치/레벨업/승급: 100 단위 반복, 승급 조건.
- 이벤트 러너: 트리거별 발동, `once` 재발동 방지, choice 분기.
- 캠페인 분기: 플래그 조합별 다음 스테이지 판정(§4.1 표 전 케이스).
- **세이브 라운드트립**: 임의 시점 `BattleState` → 직렬화 → 역직렬화 → 상태 완전 일치. `restore` 모드에서 로드 후 동일 커맨드 열 → 동일 결과(속성 기반 테스트: 랜덤 커맨드 시퀀스 100회).

### 10.2 통합/수동 테스트

- 스테이지 검수 체크리스트(§6.3)를 M4에서 스테이지마다 수행.
- 원작 대조 튜닝 세션(M2): 동일 조건(레벨·병과·지형)에서 원작 실측 데미지 vs 리메이크 산출 데미지 표를 만들어 `CFG.baseDamage` 등 상수를 회귀 보정.
- 전투 중 저장 시나리오: 저장→브라우저 강제 종료→재시작→로드 재개, 자동 저장 롤링 확인, 개조 후 구세이브 로드 폴백.

---

## 11. 장수 정보 에디터

### 11.1 요구사항

게임 내에서 아군 장수의 정보를 편집하는 공식 기능. 편집 가능 항목은 **능력치(무력/지력/통솔), 레벨, 초상화** 3종으로 한정한다(병과·아이템·경험치 등은 편집 대상 아님 — 병과는 게임 내 아이템으로, 그 외는 JSON 모딩으로).

### 11.2 진입점과 대상

- 진입: 중간 메뉴(월드맵) → "장수 에디트". 전투 중에는 진입 불가(밸런스 붕괴 방지 및 상태 일관성). 타이틀 화면에서도 진입 가능(캠페인 시작 전 편집용 — 이 경우 대상은 전체 장수).
- 대상: 아군 장수 전원(전사자 제외).
- **편집 결과는 전역 적용된다(설계 결정)**: 능력치·레벨·초상화 모두 사용자 오버레이 파일 `officers-user.json`에 기록되고, 데이터 로더가 기본 `officers.json` 위에 필드 단위로 병합해 읽는다(오버레이 우선). 따라서 진행 중인 캠페인과 **이후 새로 시작하는 캠페인 모두**에 적용된다. 기본 데이터 파일 자체는 수정하지 않으므로 오버레이 파일만 지우면 언제든 원작 값으로 복원된다("전체 초기화" 버튼 제공).
- 항목별 전역 적용 의미:
  - **능력치(무/지/통)**: 장수의 기본 능력치를 덮어쓴다. 능력치는 항상 장수 데이터에서 파생 계산되므로, 진행 중 캠페인·기존 세이브 로드 시에도 즉시 반영된다.
  - **레벨**: 두 갈래로 적용한다. ① 현재 캠페인의 해당 장수 레벨을 즉시 변경(§11.3의 재계산 규칙) ② 오버레이의 `joinLevel`을 갱신해 새 캠페인에서 그 장수가 합류할 때의 시작 레벨을 변경. 다른 기존 세이브의 레벨은 각자의 진행 상태이므로 소급 변경하지 않는다.
  - **초상화**: 초상 ID 매핑을 덮어쓴다. 모든 캠페인·세이브에 즉시 반영.
- 오버레이 저장 위치는 사용자 데이터 영역(Tauri: `appDataDir()/user-data/` — 플랫폼별 자동 해석, 개발: IndexedDB)이며 `AssetStore` 인터페이스로 추상화한다. **오버레이는 §2.6의 `dataHash` 계산에 포함하지 않는다**(에디터 사용이 세이브 호환성 경고를 유발하지 않도록). 로드 시 오버레이의 참조 무결성은 별도 검사하고, 잘못된 값은 기본값 폴백 + 경고 로그.

### 11.3 능력치·레벨 편집 규칙

| 항목 | 범위 | 반영 규칙 |
|---|---|---|
| 무력/지력/통솔 | **1~100 정수** (OfficerSchema와 동일 제한) | 즉시 실효 공격/방어·책략 명중에 반영. 지력 변경 시 책략치 상한 재계산 |
| 레벨 | **1~99 정수** | 현재 캠페인: 변경 시 **경험치는 유지**(레벨과 경험치는 독립 항목으로 취급 — 보유 경험치 0~99 그대로, 이후 100 도달 시 통상 레벨업). 병력 상한을 `growth.hpPerLevel` 기준으로 재계산하고 현재 병력은 새 상한으로 회복, 책략치 상한 재계산. 승급 조건(`upgradeLevel`) 미달로 내려가도 이미 승급한 병과는 유지. 전역: 오버레이 `joinLevel` 갱신(새 캠페인 합류 시작 레벨, §11.2) |

- 입력 UI는 스피너+직접 입력. 범위 밖 값은 입력 단계에서 차단(스키마 검증을 UI와 저장 시 이중으로 수행).
- 저장 전 변경 요약(이전→이후) 미리보기와 "되돌리기" 제공. 저장 시 자동 저장 슬롯에 백업 1회.

### 11.4 초상화 편집 규칙

- **내장 이미지 선택**: `portraits.json`(+사용자 오버레이)에 등록된 전체 초상 그리드에서 선택. 다른 장수와의 중복 사용 허용.
- **신규 업로드**: PNG/JPG/WebP 지원. 업로드 시 자동으로 표준 크기(기본 96×96, `game-config.json`로 변경 가능)에 맞춰 중앙 크롭+리사이즈하고, 크롭 영역은 사용자가 조정 가능.
  - 저장 위치: Tauri 환경 `appDataDir()/user-portraits/…`(플랫폼별 자동 해석) / 개발(브라우저) 환경 IndexedDB. `SaveStore`와 같은 패턴의 `AssetStore` 인터페이스로 추상화.
  - 등록: `portraits-user.json` 오버레이에 `{ id, source: "user", file }`로 추가. 로더는 기본 `portraits.json` 위에 오버레이를 병합해 읽는다(동일 ID는 오버레이 우선).
- 삭제: 사용자 업로드 초상은 에디터에서 제거 가능. 사용 중인 장수가 있으면 경고 후 해당 장수를 기본 초상으로 폴백.
- 세이브 파일에는 초상 ID만 저장한다. 세이브 로드 시 ID가 존재하지 않으면(파일 삭제 등) 기본 초상 폴백 + 경고 로그(§2.6 폴백 규칙과 동일 패턴).

### 11.5 구현 노트

- 화면은 HTML UI(§상위 문서 6.2)로 구현 — 폼·그리드·파일 업로드 모두 DOM이 가장 저렴하다.
- 파일 업로드는 브라우저 `<input type=file>` / Tauri 파일 다이얼로그를 같은 인터페이스로 감싼다.
- 에디터 저장 로직은 `core/campaign/officerEditor.ts`(순수 함수: 검증→적용→파생치 재계산)로 분리해 단위 테스트 대상으로 한다.

### 11.6 테스트 항목 (§10.1에 추가)

- 능력치/레벨 경계값(0, 1, 100, 101 / 0, 1, 99, 100) 검증 차단 확인.
- 레벨 변경 시 병력·책략치 상한 재계산 정확성(growth 공식 대조), 보유 경험치가 변경 전후 동일하게 유지되는지 확인.
- 오버레이 병합: `officers-user.json` 필드 단위 병합 우선순위(능력치·joinLevel·초상), 오버레이 삭제 시 원작 값 복원, 잘못된 참조 폴백.
- 전역 적용 시나리오: 에디터 수정 → 새 캠페인 시작 시 수정된 능력치·joinLevel로 합류 확인, 기존 세이브 로드 시 능력치·초상만 반영되고 레벨은 세이브 값 유지 확인.
- 에디터 적용 → 세이브 → 로드 라운드트립에서 수정값 보존. 오버레이가 `dataHash` 경고를 유발하지 않는지 확인.

---

## 12. 빌드 자동화 (윈도우·맥)

### 12.1 방침

윈도우·맥 모두 보유하고 있으나, 릴리스 빌드는 **GitHub Actions로 양 OS를 동시에 자동 빌드**하는 것을 표준 경로로 한다. 로컬 수동 빌드는 디버깅·실기 검증용 보조 수단이다. 이유: 두 OS에서 각각 수동 빌드하는 반복 작업 제거, 빌드 환경 재현성 확보(로컬 환경 오염과 무관하게 항상 같은 결과), 버전 태그와 산출물의 1:1 대응.

### 12.2 워크플로 설계 (`.github/workflows/release.yml`)

- **트리거**: `v*` 태그 푸시(릴리스 빌드) + 수동 실행(`workflow_dispatch`). 매 커밋 빌드는 하지 않는다(맥 러너는 소모 분수가 10배라 낭비).
- **잡 구성(매트릭스)**:

| 러너 | 산출물 |
|---|---|
| `windows-latest` | NSIS 설치본(`.exe` setup) + 포터블 실행 파일 |
| `macos-latest` (Apple Silicon) | `.app`을 담은 `.dmg` — `universal-apple-darwin` 타깃으로 인텔/애플실리콘 유니버설 빌드 |

- **각 잡의 단계**: checkout → Node 셋업(+npm 캐시) → Rust 셋업(+cargo 캐시) → `npm ci` → `npm run validate && npm test`(테스트 실패 시 빌드 중단) → `tauri-apps/tauri-action`으로 빌드 → 산출물을 GitHub Release 초안에 업로드.
- **서명**: 하지 않는다(개인 사용). 맥 산출물에는 Gatekeeper 우회 안내(우클릭→열기, 또는 `xattr -cr` 명령)를 Release 노트 템플릿에 자동 포함.
- **사전 검증 워크플로(선택)**: PR/푸시 시 `ubuntu-latest`에서 `validate`+`test`만 도는 경량 CI를 별도로 둔다(리눅스 러너가 가장 저렴하고, 로직 테스트는 OS 무관).

### 12.3 리포지토리 전제

- GitHub 프라이빗 리포지토리 사용(개인 프로젝트 — 공개하지 않음). 프라이빗 리포의 무료 Actions 분수 한도 내에서 태그 릴리스 빌드만 돌리면 충분하다.
- `reference/`(원작 캡처 등 개인 참고 자료)는 `.gitignore`로 리포지토리에서 제외(§6.4 방침 유지) — CI 빌드에 불필요하며 저작물 자료를 리포에 올리지 않는다.
- 버전 규칙: `tauri.conf.json`·`package.json` 버전과 git 태그를 일치시킨다(태그 푸시 전 버전 범프 스크립트 `npm run release:tag` 제공).

### 12.4 M6 완료 기준과의 연결

M6의 완료 기준은 "태그 푸시 한 번으로 GitHub Release에 윈도우·맥 설치 파일이 자동 생성되고, 각 OS 실기에서 내려받아 더블클릭 실행이 확인되는 것"으로 한다. 실기 검증 체크: 창/전체화면 전환, 세이브 경로 생성(`appDataDir()`), 초상화 업로드, BGM 자동재생(맥 WKWebView 오디오 정책), 키보드 입력(맥 Cmd 조합 충돌 여부).

---

*본 문서의 `[검증]` 항목은 M2(전투 완성)와 M4(콘텐츠 입력) 중 원작 대조로 확정하고, 확정값은 데이터 파일과 본 문서에 반영한다.*
