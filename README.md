# HERO — 삼국지 영걸전 리메이크 (YGJ-Remake)

DOS용 「삼국지 영걸전」(KOEI, 1995)의 게임 시스템과 플레이 감각을 윈도우·맥에서 실행 가능한 프로그램으로 재구현하는 개인 프로젝트.

> **개인 플레이 전용** — 배포하지 않으며, 원작 파일·에셋을 추출하거나 포함하지 않는다(하이브리드 리메이크: 엔진 신규 구현 + 커뮤니티 자료 기반 데이터 입력).

## 현재 상태

- **요구사항·설계 기준선 v1 승인** (2026-08-17)
- **M0 뼈대 완료** — 데이터 스키마·검증 파이프라인과 타일맵 렌더가 동작한다. `npm run dev`로 테스트 전장이 렌더되고 `npm run validate`가 데이터 참조 오류를 잡는다.
- **M1 전투 프로토타입 완료** — `npm run dev`로 테스트 전장에서 전투 한 판을 승패까지 완주할 수 있다. 이동(지형 코스트)·근접/원거리 공격·병과 상성·턴 교대·1단계 적 AI·승패 판정이 동작하며, 전투 난수는 결정론적이다(같은 시드 = 같은 전투).
- 다음: M2 전투 완성(책략·아이템·사기·경험치·일기토·원작 대조 튜닝)

## 핵심 특징 (계획)

- **원작 재현**: 턴제 전투(반격 없음), 병과 상성(보<기<궁<보, 방어 ±25%), 책략(지형·날씨 게이트), 일기토, 사실/가상 루트 분기, 원작 동일 맵(타일 단위 재현, 간략화 금지)
- **편의 개선**: 전투 중 턴 중간 세이브/로드(조조전식, RNG 상태 포함), 게임 내 장수 에디터(능력치·레벨·초상화, 사용자 오버레이 전역 적용)
- **개조 용이**: 모든 게임 콘텐츠가 `data/` JSON — 병종을 추가·수정하면 엔진 코드 수정 없이 데이터와 UI가 자동 반영. `npm run validate`로 참조 무결성 전수 검사

## 기술 스택

TypeScript(strict) · Vite · PixiJS v8(맵/유닛 렌더) · HTML+CSS DOM 오버레이(메뉴 UI) · Zustand · zod · Vitest · Tauri v2(윈도우·맥 패키징) · Howler.js

## 문서 지도

| 문서 | 내용 |
|---|---|
| [yeonggeoljeon-remake-design.md](./yeonggeoljeon-remake-design.md) | 선행 명세 — 전체 설계 v1.1 (프로젝트 개요, 시스템 분석, 데이터 아키텍처, 로드맵) |
| [yeonggeoljeon-remake-spec-detail.md](./yeonggeoljeon-remake-spec-detail.md) | 선행 명세 — 상세 스펙 v1.1 (시스템별 딥 스펙. 충돌 시 이 문서가 우선) |
| [docs/requirements.md](./docs/requirements.md) | 요구사항 기준선 v1 — 기능 21건, 비기능 8건, 인수 조건 12건 |
| [docs/design.md](./docs/design.md) | SW 설계 기준선 v1 — 설계 요소 14건, 데이터 계약, 검증 전략 |
| [docs/decisions.md](./docs/decisions.md) | 결정 등록부 — ADR 전체 목록 |
| [docs/plan.md](./docs/plan.md) | 구현 계획 — 마일스톤 사이클별 작업 목록과 계획 트리 |
| [docs/work/](./docs/work/) | 작업별 기록 — 설계 결정(ADR·DCR)과 구현·검증 기록 |

## 로드맵

| 마일스톤 | 내용 |
|---|---|
| ~~M0~~ ✅ | 프로젝트 뼈대: Vite+TS+PixiJS 스캐폴딩, zod 스키마, validate 스크립트 |
| ~~M1~~ ✅ | 전투 프로토타입: 타일맵, 이동/공격, 상성, 턴 교대, 1단계 AI |
| M2 | 전투 완성: 책략, 아이템, 사기·혼란, 경험치·승급, 일기토, 이벤트, 실측 튜닝 |
| M3 | 캠페인 루프: 씬 전환, 이벤트/분기, 상점·편성, 세이브(턴 중간 포함) |
| M3.5 | 맵 트레이싱 도구: 원작 스크린샷 오버레이 기반 인브라우저 맵 에디터 |
| M4 | 콘텐츠 입력: 전 시나리오 데이터·원작 동일 맵(양 루트 엔딩까지) |
| M4.5 | 장수 에디터: 능력치·레벨·초상화 편집, 오버레이 전역 적용 |
| M5 | 비주얼 패스: 플레이스홀더 → 취향 그래픽·사운드 교체 |
| M6 | 패키징: Tauri 통합, 윈도우 exe·맥 .dmg, GitHub Actions 릴리스 자동화 |

## 개발

Node.js LTS(v24 기준)가 필요하다.

```bash
npm install
npm run dev       # 브라우저에서 실행 (http://localhost:5173)
npm run validate  # 데이터 스키마·참조 무결성 전수 검사 (문제 시 exit 1)
npm test          # 코어 로직 단위 테스트 (Vitest)
npm run build     # 타입 검사 + 프로덕션 빌드
```

`npm run validate -- <경로>`로 임의의 데이터 폴더를 검사할 수도 있다.

### 전투 조작

`npm run dev`로 열면 테스트 전장의 전투가 바로 시작된다. 아군은 배치 구역 앞에서부터 자동으로 세워진다(편성·배치 화면은 M3 범위).

| 조작 | 마우스 | 키보드 |
|---|---|---|
| 커서 이동 | 마우스 이동 | 방향키 |
| 결정 (부대 선택 → 이동 → 대상 선택) | 좌클릭 | `Z` 또는 `Enter` |
| 취소 (이동 되돌리기 · 대상 선택 해제) | 우클릭 | `X` 또는 `ESC` |
| 행동 고르기 (공격·대기) | 메뉴 버튼 클릭 | 방향키로 이동 후 `Z` |

한 부대는 페이즈마다 이동 1회와 행동 1회를 쓰며 순서는 이동 → 행동으로 고정된다. **행동을 확정하기 전까지는 취소로 이동을 되돌릴 수 있다.** 행동을 마친 부대는 흐리게 표시된다. 대상을 고르는 동안 화면 우측 상단에 예상 데미지 폭이 뜨고, 실제 결과는 항상 그 안에 들어온다. 아군 부대가 모두 행동하면 적 페이즈로 자동 전환되고, 적을 전멸시키면 **승리** · 유비를 잃으면 **패배**가 표시된다. 별도의 턴 종료 조작은 없다.

전투 난수는 고정 시드를 쓰므로 같은 조작은 언제나 같은 전투가 된다(캠페인 시드 연결은 M3 범위).

### 데이터 개조

게임 콘텐츠는 전부 [data/](./data/)의 JSON이며 엔진은 이를 해석만 한다. 병과·지형·상성·스프라이트 매핑을 고친 뒤 `npm run validate`로 참조 누락을 확인하면 된다. 스프라이트 시트가 없는 항목은 플레이스홀더 색으로 렌더되므로 그래픽 없이도 시스템을 완성할 수 있다.

## 저작권 고지

원작 「삼국지 영걸전」의 저작권은 KOEI(현 코에이 테크모)에 있다. 본 저장소는 원작 게임 파일·에셋을 포함하지 않으며, 개인 학습·플레이 목적으로만 사용한다. 원작 대조용 개인 참고 자료(`reference/`)는 git에서 제외한다.
