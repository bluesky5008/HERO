# 삼국지 영걸전 리메이크 — 전체 설계 문서 (v1.1)

> **프로젝트 코드명**: YGJ-Remake
> **작성일**: 2026-08-16 (v1.1 갱신: 동일)
> **용도**: 클로드 코드(Claude Code)에서 구현을 진행하기 위한 요구사항·설계 기준 문서
> **배포 계획**: 없음 (개인 플레이 전용)
> **동반 문서**: `yeonggeoljeon-remake-spec-detail.md` (상세 스펙 — 시스템별 딥 스펙)
>
> **v1.1 변경 사항**
> 1. 조조전식 **턴 중간 세이브/로드**를 시스템 요구사항으로 추가 (원작 영걸전은 월드맵에서만 저장 가능했던 것을 개선) — §1.1, §3.6, §4.9, §7
> 2. 인게임 맵은 간략화 없이 **원작과 동일하게 재현**하는 것으로 방침 확정. 이를 위한 맵 트레이싱 도구를 로드맵에 추가 — §2.7, §8, §9
> 3. 상세 스펙 문서(딥 스펙) 분리 작성

---

## 1. 프로젝트 개요

### 1.1 목표

DOS용 「삼국지 영걸전」(KOEI, 1995)의 게임 시스템과 플레이 감각을 윈도우에서 실행 가능한 프로그램으로 재구현한다.

- **유지할 것**: 원작의 데이터(무장·병과·능력치·시나리오 흐름)와 플레이 방식(턴제 전투, 병과 상성, 책략, 일기토, 사실/가상 분기). **인게임 맵은 지형 배치·유닛 배치를 원작과 동일하게 재현**한다(간략화 금지 — 간략화 시 원작 디테일 유실).
- **바꿀 것**: 타일맵 그래픽, 부대 조형(스프라이트), 애니메이션 등 시각 요소는 취향에 따라 자유롭게 교체·수정. 단 시각 요소를 바꾸더라도 맵의 논리 구조(지형 종류·좌표)는 원작 기준을 유지한다.
- **개선할 것(원작 대비 편의 기능)**: 삼국지 조조전과 유사한 **전투 중 턴 중간 세이브/로드**를 지원한다. 원작 영걸전은 월드맵 상태에서만 저장·로드가 가능했고 전투 중 저장이 불가능했는데, 이 리메이크에서는 전투 중 임의 시점 저장을 시스템 요구사항으로 포함한다. 또한 **장수 정보 에디트 기능**을 내장한다 — 능력치(무/지/통)·레벨·초상화를 게임 내에서 수정 가능하며, 능력치와 레벨은 기존 숫자 제한(능력치 1~100, 레벨 1~99)을 따르고, 초상화는 내장 이미지 선택 또는 신규 업로드를 지원한다. 편집 결과는 사용자 오버레이 파일로 **전역 적용**되어 진행 중 캠페인과 새 캠페인 모두에 반영되며, 오버레이 삭제로 언제든 원래 값으로 복원 가능하다(상세 스펙 §11).
- **핵심 요구사항**: 일부 부대·병종을 수정/추가하면 **데이터와 UI가 함께 자동 반영**되는 구조

### 1.2 진행 방식

- **하이브리드 리메이크**: 엔진(게임 로직, 렌더링, UI)은 새로 구현. 데이터는 커뮤니티 공략/위키 자료를 참고해 JSON으로 입력. 원작 파일 추출은 하지 않음.
- 설계는 본 문서에서 확정, 구현은 클로드 코드에서 마일스톤 단위로 진행.

### 1.3 설계 원칙

1. **데이터 주도(Data-Driven)**: 모든 게임 콘텐츠(무장, 병과, 상성, 책략, 아이템, 맵, 이벤트, 스프라이트 매핑, 애니메이션 정의)는 코드가 아닌 `data/` 폴더의 JSON 파일에 존재한다. 엔진 코드는 데이터의 "해석기"일 뿐이다.
2. **스키마 검증**: 모든 데이터 파일은 스키마(zod)로 검증한다. 병종을 추가했는데 스프라이트 매핑이 빠졌다면 실행 전에 에러 목록으로 알려준다.
3. **결정론적 로직**: 전투 계산은 시드 기반 난수를 사용해 동일 입력 → 동일 결과를 보장한다(테스트와 밸런스 튜닝을 위해).
4. **로직과 렌더링의 분리**: 전투 규칙은 렌더링 없이 단독 실행 가능한 순수 TypeScript 모듈로 만든다. PixiJS는 그 결과를 그리기만 한다.
5. **원작 우선, 개조 허용**: 기본값은 원작 재현. 다만 모든 수치는 데이터 파일에 있으므로 언제든 취향대로 개조 가능하다.

### 1.4 기술 스택 (확정)

| 영역 | 선택 | 비고 |
|---|---|---|
| 언어 | TypeScript (strict) | 전 영역 공통 |
| 빌드/개발 서버 | Vite | 핫 리로드로 빠른 반복 |
| 맵/유닛 렌더링 | PixiJS v8 | Canvas/WebGL 2D 렌더러 |
| 메뉴/상태창/대화 UI | HTML + CSS (DOM 오버레이) | SRPG의 방대한 UI에 유리 |
| 상태 관리 | Zustand (경량) | 게임 상태 ↔ UI 동기화 |
| 데이터 검증 | zod | JSON 스키마 정의 겸 검증 |
| 테스트 | Vitest | 전투 로직 단위 테스트 |
| 데스크톱 패키징 | Tauri v2 | **윈도우(exe)·맥(.app) 양쪽 지원**. 완성 단계에서 플랫폼별 빌드, 세이브 파일 디스크 저장 |
| 사운드 | Howler.js | BGM/효과음 |

개발 중에는 브라우저(`npm run dev`)에서 플레이하고, 완성 시 Tauri로 패키징한다. **윈도우와 맥 양쪽에서 실행 가능**하다: 코드베이스는 하나이며 빌드는 GitHub Actions 매트릭스로 **양 OS를 동시에 자동 빌드**한다(상세 스펙 §12). 로컬 수동 빌드(각 OS에서 `npm run tauri build`)도 병행 가능하다. 맥은 서명 없이 개인 사용 가능(첫 실행 시 우클릭→열기). 세이브는 개발 중 localStorage → 패키징 후 Tauri fs API로 전환하며, 저장 경로는 Tauri `appDataDir()`로 플랫폼별 자동 해석된다(저장소 인터페이스를 추상화해 교체 가능하게 설계).

---

## 2. 원작 시스템 분석 요약

커뮤니티 위키(나무위키/더위키) 조사 결과를 기준으로 한다. 구현 중 세부 수치가 불명확한 항목은 "커뮤니티 추정치"로 데이터에 입력하고 플레이하며 튜닝한다.

### 2.1 기본 구조

- 턴제 SRPG. 플레이어 턴 → 적 턴 교대. **반격 없음**(일부 특수 능력 제외)이 원작의 큰 특징.
- 유비군 시점으로 황건적의 난부터 시나리오를 따라 진행. 장(章) 단위 구성, 전투 사이에 이동/이벤트/상점 파트가 있다.
- 특정 전투의 승패·선택지에 따라 **사실 루트/가상 루트**로 분기하고, 잘못된 선택지는 즉시 게임 오버가 되기도 한다.

### 2.2 병과 체계

- **공격계 3계열**: 보병(단병→장병→전차대), 기병(경기병→중기병→친위대), 궁병(궁병→연노병→발석차)
- **보조계 3종**: 수송대(병력 회복), 군악대(사기 회복), 주술사(거의 모든 책략 사용)
- **특수계**: 적병 등. 산 이동 가능, 전직 불가.
- **상성**: 보병 < 기병 < 궁병 < 보병 의 가위바위보. 상성은 **방어력 ±25% 보정**으로 작동.
- 병과별 특징: 보병은 최고 방어력·8방향 공격(장병 이상), 기병은 최고 공격력·기동력이지만 숲 진입 불가·4방향 공격, 궁병은 원거리·대기병 우위지만 상위 병과일수록 이동력 감소.
- 승급: 아이템(장창, 보병차, 근위개 등) + 레벨 조건(예: 장병 전직 Lv15, 전차대/친위대 Lv30).

### 2.3 부대 능력치

- 장수 능력치: **무력, 지력, 통솔력**
- 부대 수치: **병력(HP), 사기, 책략치(MP), 공격력, 방어력, 이동력**
- 커뮤니티 분석 공식(검증 대상):
  `실효 능력치 = (레벨 + 10) × (사기 + 기본 능력치 + 병과 보정) × 보물 보정`
- 사기가 낮으면 공방이 크게 떨어지고 혼란에 빠질 수 있다. 병력이 1이라도 사기와 능력치가 높으면 큰 데미지를 낼 수 있는 구조(병력은 데미지 계수에 직접 곱해지지 않음).
- 거점 지형(성, 마을)에서 턴마다 병력·사기 소량 회복.

### 2.4 책략

- 공격계: **화계**(초열→화룡→업화→대초열→대화룡, 보병계·주술사), **수계**(소용돌이→탁류→해일→대소용돌이→대탁류, 궁병계·주술사), **지계**(낙석→산사태→토석류→대낙석→대산사태, 주술사·적병계)
- 보조계: 사기저하(견제/도발/위압), 혼란(거짓정보/위병/위장)
- 회복계: 병력 회복(원조/보급/구제/대원조/대보급/대구제, 수송대·주술사), 사기 회복(격려/응원/고무/대격려/대응원/대고무, 군악대·주술사), 병력+사기(간호/치유/구명, 주술사)
- 조건: 화계는 숲에서 +25%·우천 시 사용 불가, 수계는 우천 시 +25%·평지/다리에서만, 지계는 산/황무지에서만·날씨 무관. 거점 위 부대에게는 공격 책략 무효. "대(大)" 계열은 십자 범위 최대 5부대.
- 지력이 높을수록 명중률·피해 증가. 회복 책략은 항상 성공.

### 2.5 아이템

- 소지 한도 8개(부대당). 카테고리: 무기(공격 +10~24%), 병법서(방어 +15~22%), 이동력(적토마 +3 등 3종), 자연 회복(옥새 등), 병과 전환서(검술/궁술/마술지침서), 병과 승급템(장창/보병차/근위개), 소모품(회복서·공격서·혼란서, 사용 시 경험치 획득).
- 자금 상한 10,000. 상점 구매 + 전투맵 군량고/보물고 획득.

### 2.6 일기토

- 특정 전투에서 조건 충족 시 장수 간 1:1 결투 이벤트 발생(대부분 스크립트로 승패 고정).
- 승리 시 대량 경험치(레벨 +1 수준). 적장 즉시 퇴각 등 전황에 큰 영향.

### 2.7 이번 리메이크에서의 취급

| 요소 | 방침 |
|---|---|
| 데이터(무장/병과/책략/아이템 수치) | 원작 기준으로 JSON 입력, 이후 취향 개조 |
| 전투 규칙(상성, 반격 없음, 책략 조건) | 원작 재현 |
| 시나리오/분기 | 원작 흐름 재현(이벤트 스크립트로 데이터화) |
| 인게임 맵(지형·배치) | **원작과 동일 재현**. 스크린샷/영상 대조로 타일 단위 트레이싱. 간략화 금지 |
| 세이브 | **개선**: 월드맵 세이브(원작) + 전투 중 턴 중간 세이브(조조전식, 신규) |
| 타일맵/부대 스프라이트/애니메이션 | **자체 제작·교체 전제**. 플레이스홀더 → 취향 리소스로 단계적 교체(논리 구조는 원작 유지) |
| 해상도/UI 레이아웃 | 현대화(1280×720 기준, 스케일링 지원) |

---

## 3. 게임 시스템 설계

### 3.1 게임 전체 흐름 (상태 기계)

```
[타이틀] → [새 게임 / 불러오기]
   ↓
[캠페인 루프]
   ├─ [이벤트 씬] 대화, 선택지, 인물 가입/이탈
   ├─ [중간 메뉴] 부대 편성, 아이템 정리, 상점, 세이브
   ├─ [전투 씬] ← 본 게임의 핵심
   │    ├─ 배치 → (플레이어 턴 ⇄ 적 턴)* → 승리/패배
   │    └─ 전투 중 이벤트(증원, 일기토, 대사) 트리거
   └─ [분기 판정] 승패·선택지·플래그 → 다음 스테이지 결정
   ↓
[엔딩]
```

캠페인 진행 상태는 `CampaignState`(현재 스테이지 ID, 보유 무장 목록, 자금, 아이템, 플래그 집합)로 표현하고, 시나리오 그래프(§4.7)를 따라 전이한다.

### 3.2 전투 씬 규칙

- 격자 맵(정사각 타일). 원작 맵 크기를 참고하되 데이터로 정의(예: 40×40 내외).
- 턴 구조: 플레이어가 모든 부대를 행동(이동+행동 1회) → 적 AI 턴. 행동: 공격 / 책략 / 아이템 / 대기.
- **반격 없음**(원작 재현). 특수 병과의 반격 능력은 병과 데이터의 플래그로 지원.
- 지형: 평지, 숲, 산, 황무지, 강, 다리, 마을, 성문, 성벽, 군량고/보물고 등. 지형별 이동 코스트·방어 보정·책략 조건을 `terrain.json`에 정의.
- 날씨: 맑음/비. 화계·수계 조건에 관여. 스테이지 데이터에 고정 또는 확률 정의.
- 승리 조건(스테이지별 데이터): 적 전멸 / 특정 부대 격파 / 특정 지점 도달 / N턴 버티기. 패배 조건: 유비 부대 괴멸(즉시 게임 오버) 등.
- 사기: 피격·책략으로 감소, 군악대·아이템·거점으로 회복. 일정 이하이면 혼란(행동 불가/랜덤 이동) 판정.
- 경험치: 공격/책략/회복 행동 및 격파 시 획득, 100 단위로 레벨업. 레벨업 시 병력·책략치 상한 증가. 아군 격파 유닛의 경험치 독식 구조(원작 재현).

### 3.3 데미지 공식 (초기 구현값)

원작의 정확한 내부 공식은 비공개이므로, 커뮤니티 분석 공식을 기반으로 아래를 **초기값**으로 구현하고 상수를 `combat-config.json`으로 외부화해 튜닝한다.

```
공격측 실효공격 = (레벨+10) × (사기 + 무력 + 병과공격보정) × (1 + 무기보정%)
방어측 실효방어 = (레벨+10) × (사기 + 통솔 + 병과방어보정) × (1 + 병법서보정%)
                × 상성보정(±25%) × (1 + 지형방어보정%)

물리 데미지 = BASE × 실효공격 / 실효방어 × 난수(0.9~1.1)
책략 데미지 = 책략기본치 × f(지력차) × 지형/날씨보정(±25%) × 난수
책략 명중률 = clamp(g(시전자 지력, 대상 지력), min, max)   // 회복계는 100%
```

`BASE`, `f`, `g`, 사기 감소량, 혼란 임계값 등 모든 상수는 설정 파일에 둔다. **밸런스 검증 마일스톤(M2)에서 원작 플레이 영상/공략의 실측 데미지와 대조해 보정한다.**

### 3.4 적 AI (단계적 구현)

- **1단계(M1)**: 가장 가까운 아군에게 접근해 공격. 사거리 밖이면 이동만.
- **2단계(M2)**: 스테이지 데이터의 AI 프로필 적용 — `aggressive`(돌격) / `defensive`(거점 사수, 접근 시만 반응) / `guard`(지정 위치 고수) / `support`(회복 우선). 타깃 선정 가중치: 예상 데미지, 격파 가능성, 대상의 위협도.
- **3단계(선택)**: 책략 지형 활용, 집중 공격 로직.

### 3.5 일기토·전투 이벤트

전투 중 이벤트는 트리거-액션 스크립트로 데이터화한다.

- 트리거 예: `unitAttacks(관우, 화웅)`, `turnStart(5)`, `unitHpBelow(적장, 30%)`, `unitReaches(유비, 영역)`
- 액션 예: `dialogue(...)`, `duel(관우, 화웅, 결과=승리고정)`, `spawnUnits(증원)`, `setFlag(...)`, `endBattle(승리)`
- 일기토는 결과 고정형(원작 대부분)과 능력치 판정형(무력+난수) 두 방식을 지원. 연출은 컷인 + 스프라이트 애니메이션.

### 3.6 세이브/로드 규칙 (조조전식 턴 중간 세이브 포함)

| 구분 | 저장 가능 시점 | 내용 |
|---|---|---|
| 월드맵 세이브 | 중간 메뉴(전투 사이) | `CampaignState` 전체. 원작과 동일한 개념 |
| **전투 중 세이브 (신규)** | 플레이어 턴 중 임의 시점(전투 메뉴에서) | `CampaignState` + `BattleState` 스냅숏 + **난수 상태(RNG state)** |
| 자동 저장 | 매 턴 시작 시(전투 중), 전투 개시 직전 | 롤링 슬롯. 크래시·실수 복구용 |

- 전투 중 세이브를 로드하면 정확히 그 시점(이동/행동 완료 상태, 이벤트 플래그, 날씨, 턴 수)부터 재개된다.
- **난수 상태를 세이브에 포함**한다. 결정론적 RNG(§1.3)와 결합하면 "로드 → 같은 행동 → 같은 결과"가 보장된다. 세이브/로드 반복으로 결과를 되굴리는 플레이(조조전에서 흔한 리세마라)를 허용할지 여부는 옵션으로 제공한다: `rngOnLoad: "restore"(결과 고정) | "reseed"(로드 시 재추첨, 기본값)`. 개인 취향에 맞게 설정 가능.
- 이벤트 연출·애니메이션 재생 중에는 저장 불가(상태 일관성 보장). 적 턴 중에도 저장 불가.
- 상세 스펙(직렬화 포맷, 슬롯 구성, 무결성 검증, 데이터 개조 후 호환성)은 상세 스펙 문서 §2 참조.

---

## 4. 데이터 아키텍처

`data/` 폴더 구성. 모든 파일은 zod 스키마로 검증되며, `npm run validate`가 참조 무결성(존재하지 않는 병과 ID 참조, 스프라이트 누락 등)을 전수 검사한다.

```
data/
├─ config/
│  ├─ combat-config.json      # 데미지 상수, 사기/혼란 임계값, 경험치 테이블
│  └─ game-config.json        # 자금 상한, 소지품 한도, 그리드/타일 크기
├─ classes.json               # 병과 정의 (핵심 파일)
├─ affinity.json              # 병과 상성표
├─ officers.json              # 무장 데이터
├─ tactics.json               # 책략 정의
├─ items.json                 # 아이템 정의
├─ terrain.json               # 지형 타입 정의
├─ scenario/
│  ├─ campaign.json           # 스테이지 그래프(분기 포함)
│  ├─ stages/stage-001.json…  # 스테이지별 맵/배치/승패조건/이벤트
│  └─ dialogues/…             # 대화 스크립트
└─ assets-map/
   ├─ sprites.json            # 병과·장수 → 스프라이트 시트 매핑
   ├─ animations.json         # 애니메이션 정의(프레임 시퀀스)
   └─ portraits.json          # 초상화 매핑
```

### 4.1 병과 (`classes.json`) — 커스터마이징의 중심

```jsonc
{
  "id": "light_cavalry",
  "name": "경기병",
  "family": "cavalry",            // 상성 판정용 계열
  "tier": 1,                      // 승급 단계
  "upgradesTo": "heavy_cavalry",  // null이면 최종 승급
  "upgradeLevel": 15,
  "attackMod": 12, "defenseMod": 6,
  "movement": 6,
  "attackRange": { "min": 1, "max": 1, "directions": 4 },
  "movementRules": { "forbidden": ["forest"], "halved": [] },
  "tactics": [],                  // 사용 가능 책략 카테고리/ID
  "flags": [],                    // "counterAttack", "mountainMove" 등
  "sprite": "light_cavalry"       // sprites.json의 키
}
```

**병종 추가/수정 시나리오**: 새 병종 "연노기병"을 만들려면 ① `classes.json`에 항목 추가 ② `affinity.json`에 계열 상성 확인/추가 ③ 스프라이트 시트에 그림 추가 후 `sprites.json`·`animations.json` 매핑 ④ (기존 무장에게 주려면) `officers.json`의 `classId` 변경. `npm run validate`가 누락을 잡아주고, 게임 내 모든 UI(맵 유닛, 상태창, 편성 화면, 전투 로그)는 이 데이터를 읽어 자동 반영된다. **엔진 코드 수정 불필요.**

### 4.2 상성 (`affinity.json`)

계열×계열 행렬. 값은 방어력 보정 배수(원작: 유리 시 상대 방어 -25% 효과).

```jsonc
{ "attacker": "cavalry", "defender": "infantry", "modifier": 1.25 }
```

### 4.3 무장 (`officers.json`)

```jsonc
{
  "id": "guan_yu", "name": "관우",
  "war": 97, "int": 83, "ldr": 95,          // 무력/지력/통솔
  "classId": "heavy_cavalry",
  "joinStage": "stage-001", "leaveStage": null,
  "portrait": "guan_yu",
  "duelProfile": { "strength": 97, "special": true },
  "growth": { "hpPerLevel": 50, "mpCurve": "int-based" }
}
```

### 4.4 책략 (`tactics.json`)

```jsonc
{
  "id": "fire_dragon", "name": "화룡", "category": "fire",
  "cost": 8, "baseDamage": 600,
  "range": 9, "area": "single",              // single | cross5
  "terrainRequired": null,                    // 지계: ["mountain","wasteland"]
  "terrainBonus": { "forest": 1.25 },
  "weatherForbidden": ["rain"],               // 화계
  "weatherBonus": {},                         // 수계: { "rain": 1.25 }
  "effect": "damage"                          // damage|moraleDown|confuse|healHp|healMorale|healBoth
}
```

### 4.5 아이템 (`items.json`)

`type`: `weapon`(공격%) / `manual`(방어%) / `mount`(이동+n) / `regen`(턴 회복) / `classChange` / `classUpgrade` / `consumable`(책략 복제 효과). 획득처(상점 가격 또는 전투맵 보물)와 효과를 데이터로 정의.

**병과 변경/승급 아이템의 사용 조건은 병과·레벨 조건만 둔다.** 원작에 있던 특정 장수 사용 제한(유비의 병과 변경 불가)은 이 리메이크에서 채택하지 않는다 — 유비도 병과 전환서·승급 아이템을 자유롭게 사용할 수 있다(상세 스펙 §1.6).

### 4.6 스테이지 (`stages/*.json`)

```jsonc
{
  "id": "stage-001", "name": "황건적의 난 - 영천 전투",
  "map": { "width": 32, "height": 28, "tiles": "…(타일 ID 배열 or CSV)…" },
  "weather": "clear",
  "deployment": { "maxUnits": 8, "zone": [[2,20],[5,26]] },
  "enemies": [ { "officerId": "deng_mao", "classId": "sword_soldier", "level": 3, "pos": [18,6], "ai": "aggressive", "drops": [] } ],
  "victory": { "type": "defeatAll" },
  "defeat": { "type": "unitDestroyed", "unit": "liu_bei" },
  "treasures": [ { "pos": [10,3], "itemId": "rice" } ],
  "events": [ { "trigger": {...}, "actions": [...] } ],
  "shopBefore": ["rice","wine"],
  "next": "stage-002"                        // 또는 분기 객체
}
```

### 4.7 캠페인 그래프 (`campaign.json`)

스테이지를 노드로, 분기를 엣지로 갖는 그래프. 분기 조건은 플래그/선택지/승패 결과의 조합.

```jsonc
{ "from": "stage-045",
  "branches": [
    { "if": { "flag": "historical_route" }, "to": "stage-046a" },
    { "else": true, "to": "stage-046b" } ] }
```

선택지 중 즉시 게임 오버(원작 재현)도 이벤트 액션 `gameOver(사유)`로 표현한다.

### 4.8 비주얼 매핑 (`sprites.json` / `animations.json`)

```jsonc
// sprites.json — 병과 키 → 시트와 기준 프레임
{ "light_cavalry": { "sheet": "units.png", "frameSize": [32, 32], "baseRow": 4 } }

// animations.json — 상태별 프레임 시퀀스와 타이밍
{ "light_cavalry": {
    "idle":   { "frames": [0,1], "fps": 2 },
    "move":   { "frames": [2,3,4,5], "fps": 8 },
    "attack": { "frames": [6,7,8], "fps": 10, "once": true },
    "damaged":{ "frames": [9], "fps": 1, "flash": true } } }
```

타일맵도 동일 구조(`tileset.png` + `terrain.json`의 타일 ID 매핑). **그래픽 교체 = 이미지 파일과 이 두 JSON만 수정**하면 끝나도록 한다. 아군/적군 구분은 팔레트 틴트(진영 색상)로 처리해 스프라이트 1벌로 양 진영을 그린다.

### 4.9 세이브 데이터

`CampaignState` + 전투 중이라면 `BattleState` 스냅숏(유닛 위치/병력/사기/책략치/행동 여부/상태이상, 턴 수, 페이즈, 날씨, 발동된 이벤트 플래그, **RNG 상태**)을 JSON 직렬화한다. 저장 유형 필드(`worldmap` | `battle` | `auto`)와 버전 필드, 데이터 해시(개조된 데이터로 만든 세이브인지 식별)를 포함한다. 수동 슬롯 8개 + 자동 저장 롤링 3개. 상세 포맷은 상세 스펙 문서 §2.

---

## 5. 커스터마이징 설계 (핵심 요구사항 상세)

사용자 요구: *"타일맵, 부대 조형, 애니메이션을 취향대로 수정하고, 병종을 수정하면 데이터와 UI가 함께 바뀌어야 한다."*

이를 보장하는 규칙:

1. **UI는 데이터를 하드코딩하지 않는다.** 상태창의 병과명, 편성 화면의 병과 목록, 상성 안내 표시는 전부 `classes.json`/`affinity.json`을 읽어 렌더링한다. 병종이 7개에서 9개가 되어도 UI는 그대로 동작한다.
2. **스프라이트 계약(contract)**: 모든 병과는 `idle/move/attack/damaged` 4개 애니메이션을 갖는다는 것만 엔진이 요구한다. 프레임 수·크기·속도는 자유. 누락 시 validate가 경고하고, 런타임은 플레이스홀더(색상 사각형 + 병과명)로 대체 렌더링해 게임이 죽지 않게 한다.
3. **플레이스홀더 우선 전략**: 초기 개발은 단색 도형+아이콘 플레이스홀더로 전 시스템을 완성하고, 이후 취향 리소스(직접 제작, 무료 에셋, AI 생성 등)로 시트만 교체한다. 게임 완성이 그래픽 제작에 발목 잡히지 않게 하는 장치.
4. **개조 워크플로우 문서화**: `MODDING.md`에 "병종 추가 절차", "스프라이트 교체 절차", "밸런스 상수 위치"를 기록한다. 클로드 코드에게 "이 문서대로 ○○ 해줘"라고 지시하는 기준 문서가 된다.
5. **(선택) 인게임 데이터 뷰어**: 개발자 모드에서 병과/상성/책략 표를 게임 안에서 열람하는 화면. HTML UI라 저비용으로 구현 가능. 우선순위 낮음(M5 이후).
6. **장수 정보 에디터(확정 요구사항)**: 게임 내 화면에서 아군 장수의 능력치·레벨·초상화를 편집한다. 능력치·레벨은 스키마의 숫자 제한을 그대로 강제하고, 초상화는 내장 초상 선택 그리드 + 사용자 이미지 업로드를 지원한다. 편집 결과는 `officers-user.json` 오버레이로 **전역 적용**(진행 중 캠페인 + 새 캠페인)되며, 기본 데이터는 수정하지 않아 오버레이 삭제만으로 원작 값 복원이 가능하다. JSON 직접 편집(모딩)과 달리 게임을 벗어나지 않고 쓰는 공식 기능이다. 상세 스펙 §11.

---

## 6. 기술 아키텍처

### 6.1 폴더 구조

```
ygj-remake/
├─ data/                    # §4의 게임 데이터 (JSON)
├─ public/assets/           # 스프라이트 시트, 타일셋, 초상화, BGM
├─ src/
│  ├─ core/                 # 순수 로직 (PixiJS 의존 없음, 100% 테스트 대상)
│  │  ├─ battle/            # 턴 관리, 이동 범위(BFS), 데미지, 책략, 사기, AI
│  │  ├─ campaign/          # 캠페인 상태, 분기 판정, 이벤트 인터프리터
│  │  ├─ data/              # 로더 + zod 스키마 + 참조 무결성 검사
│  │  └─ rng.ts             # 시드 기반 난수
│  ├─ render/               # PixiJS 레이어
│  │  ├─ TilemapRenderer.ts # 지형 레이어
│  │  ├─ UnitRenderer.ts    # 유닛 스프라이트 + 애니메이션 상태기계
│  │  ├─ EffectRenderer.ts  # 책략 이펙트, 데미지 숫자, 컷인
│  │  └─ CameraController.ts
│  ├─ ui/                   # HTML/CSS UI (메뉴, 상태창, 대화창, 상점, 편성)
│  ├─ scenes/               # Title / Campaign / Battle / Ending 씬 전환
│  ├─ audio/
│  └─ save/                 # 저장소 추상화 (localStorage ↔ Tauri fs)
├─ scripts/validate.ts      # npm run validate
├─ tests/                   # Vitest (데미지 공식, 상성, 이동 범위, 분기 판정)
└─ src-tauri/               # M6에서 추가
```

### 6.2 렌더링 설계

- PixiJS 스테이지 레이어 순서: 지형 → 격자/이동범위 하이라이트 → 유닛 → 이펙트 → 커서.
- 논리 해상도 1280×720, 정수 배율 스케일링. 타일 크기는 `game-config.json`(기본 32px)로 정의해 취향에 따라 교체 가능.
- 유닛 애니메이션은 `animations.json`을 읽는 범용 상태 기계 하나로 처리(병과별 코드 없음).
- 게임 UI(HP바, 메뉴, 상태창, 대화창)는 PixiJS 캔버스 위 DOM 오버레이. 게임 상태는 Zustand 스토어를 통해 UI와 단방향 동기화.

### 6.3 핵심 인터페이스 (발췌)

```ts
// core/battle — 렌더링과 무관하게 동작
interface BattleState { units: Unit[]; turn: number; phase: "player"|"enemy"; weather: Weather; flags: Set<string>; }
interface Unit { officerId: string; classId: string; level: number; hp: number; morale: number; mp: number;
                 pos: Vec2; items: string[]; acted: boolean; status: "normal"|"confused"; }

// 모든 행동은 커맨드로 표현 → 로그·리플레이·테스트 용이
type Command = Move | Attack | UseTactic | UseItem | Wait;
function applyCommand(state: BattleState, cmd: Command, rng: Rng): BattleEvent[];
// BattleEvent(데미지 발생, 사기 변화, 격파, 일기토 발생…)를 렌더러가 구독해 연출 재생
```

---

## 7. 화면(UI) 설계

| 화면 | 구성 요소 |
|---|---|
| 타이틀 | 새 게임 / 이어하기 / 설정(음량, 전투 속도, 창/전체화면) |
| 이벤트 씬 | 배경 + 초상화 + 대화창, 선택지 버튼, 스킵 |
| 중간 메뉴 | 부대 목록(병과·레벨·병력), 출진 편성(스테이지 출진 수 제한), 아이템 관리(부대 간 이동), 상점, 세이브/로드 |
| 전투 — 맵 | 타일맵, 유닛, 이동 범위(청)/공격 범위(적) 하이라이트, 턴 표시, 미니 정보(지형 명·방어 보정) |
| 전투 — 명령 | 이동 후 컨텍스트 메뉴: 공격/책략/아이템/대기. 대상 선택 시 예상 데미지 표시 |
| 전투 — 시스템 메뉴 | 빈 타일 우클릭/ESC: **세이브(턴 중간)**/로드, 턴 종료, 부대 일람, 설정, 전투 포기 |
| 장수 에디터 | 중간 메뉴에서 진입. 장수 선택 → 능력치(무/지/통, 1~100)·레벨(1~99) 편집, 초상화 선택 그리드/업로드. 저장 전 변경 미리보기와 되돌리기 |
| 전투 — 상태창 | 부대 상세: 장수 초상, 무/지/통, 병력/사기/책략치, 병과(상성 요약), 소지품 |
| 일기토 | 컷인 연출 + 대사 + 결과 |
| 상점 | 구매/판매, 자금 표시 |

키보드(방향키+Z/X)와 마우스 모두 지원. 원작의 커서 이동 감각을 기본으로 한다.

---

## 8. 개발 로드맵 (클로드 코드 작업 단위)

각 마일스톤은 "브라우저에서 실행해 눈으로 확인 가능한 상태"로 끝나는 것을 원칙으로 한다.

| 마일스톤 | 내용 | 완료 기준 |
|---|---|---|
| **M0 뼈대** | Vite+TS+PixiJS 프로젝트 생성, 데이터 로더+zod 스키마, validate 스크립트, 플레이스홀더 에셋 파이프라인 | `npm run dev`로 빈 맵 렌더, `npm run validate` 동작 |
| **M1 전투 프로토타입** | 타일맵 렌더, 유닛 배치/선택/이동(BFS 이동범위), 근접 공격+데미지 공식+상성, 턴 교대, 1단계 AI, 승패 판정 | 테스트 스테이지 1개에서 전투 한 판 완주 |
| **M2 전투 완성** | 책략(전 카테고리+지형/날씨 조건), 아이템 사용/장비 효과, 사기·혼란, 경험치·레벨업·승급, 일기토, 전투 이벤트 스크립트, 2단계 AI, 거점 회복 | 원작 1장 수준의 전투를 규칙 누락 없이 재현. 데미지 실측 대조 튜닝 |
| **M3 캠페인 루프** | 씬 전환, 이벤트/대화 시스템, 선택지·분기 판정, 상점·편성 화면, 월드맵 세이브 + **전투 중 턴 세이브/로드(§3.6)** + 자동 저장 | 스테이지 3개를 이벤트 포함 연속 플레이, 전투 중 저장→종료→재개 검증 |
| **M3.5 맵 트레이싱 도구** | 원작 스크린샷을 반투명 오버레이로 깔고 격자 위에 타일을 칠하는 인브라우저 맵 에디터(참조 이미지 로드, 타일 팔레트, 유닛 배치, 원작 대조 토글, JSON 내보내기) | 도구로 원작 맵 1개를 타일 단위로 재현 완료 |
| **M4 콘텐츠 입력** | 전 시나리오 데이터 입력(무장, 스테이지 맵, 이벤트, 분기, 아이템 배치). 공략/위키/플레이 영상 기반. **맵은 M3.5 도구로 원작과 동일하게 타일 단위 재현**(간략화 금지) | 처음부터 엔딩(사실/가상 양 루트)까지 플레이 가능, 맵 검수 체크리스트 통과 |
| **M4.5 장수 에디터** | 게임 내 장수 정보 편집 화면(능력치/레벨/초상화), 초상화 업로드 파이프라인, 숫자 제한 검증(상세 스펙 §11) | 에디터로 수정한 값이 세이브에 반영되고 전 UI에 표시됨 |
| **M5 비주얼 패스** | 플레이스홀더 → 취향 그래픽 교체(타일셋, 유닛 시트, 초상화), 애니메이션·이펙트 다듬기, BGM/효과음 | "내 취향의 영걸전"으로 보이는 상태 |
| **M6 패키징** | Tauri 통합, 세이브 fs 전환, **윈도우 exe + 맥 .app 빌드**, **GitHub Actions 빌드 자동화(윈도우·맥 매트릭스, 상세 스펙 §12)**, 창/전체화면, 맥 WKWebView 실기 검증(오디오 정책·렌더링) | 태그 푸시 한 번으로 양 OS 설치 파일이 자동 생성되고, 각 OS에서 더블클릭 실행 확인 |

M4는 물량 작업이므로 M1~M3와 병행 가능하다(예: M2 진행 중에 1장 데이터 입력). 커스터마이징(병종 개조)은 M2 완료 시점부터 언제든 가능하다.

### 클로드 코드 지시 예시

- M0: "이 설계 문서 §1.4, §4, §6대로 프로젝트를 스캐폴딩하고 classes/affinity/terrain 스키마와 validate 스크립트를 만들어줘."
- M1: "§3.2~3.3 규칙으로 전투 코어를 구현하고 Vitest로 상성·이동범위 테스트를 작성해줘."
- 개조: "MODDING.md 절차대로 '연노기병' 병종을 추가하고 관우 부대에 적용해줘."

---

## 9. 데이터 수집 계획과 리스크

### 9.1 수집이 필요한 원작 데이터

| 데이터 | 출처 후보 | 난이도 |
|---|---|---|
| 무장 목록·능력치 | 나무위키/더위키, 공략 카페(다음 삼국지아카데미 등) | 낮음 |
| 병과·책략·아이템 수치 | 위키에 상세 정리됨(§2 반영) | 낮음 |
| 스테이지 목록·분기 조건 | 위키 선택지 문서 + 공략글 | 중간 |
| 맵 지형·유닛 초기 배치 | 원작 스크린샷·플레이 영상을 타일 단위로 트레이싱 | **높음(최대 물량)** |
| 이벤트 대사 | 플레이 영상/공략 | 중간 |

**맵 재현 방침(확정)**: 인게임 맵은 시간이 들더라도 원작과 동일하게 재현한다. 간략화는 원작 디테일(지형 요충지, 보물고 위치, 증원 경로 등)을 잃기 쉬우므로 하지 않는다. 이를 위해 ① M3.5에서 참조 이미지 오버레이 방식의 맵 트레이싱 도구를 먼저 만들고 ② 스테이지별 검수 체크리스트(지형 일치, 배치 일치, 보물·군량고 좌표, 증원 위치)를 운용하며 ③ 원작 대조가 어려운 부분은 "미확정" 마킹 후 플레이 영상으로 재검증한다. 작업 절차 상세는 상세 스펙 문서 §6.

### 9.2 리스크와 대응

| 리스크 | 대응 |
|---|---|
| 데미지 공식이 원작과 미묘하게 다름 | 상수 외부화 + 실측 대조 튜닝(M2). "원작 감각"이 목표이지 바이너리 일치가 목표가 아님 |
| 그래픽 제작 부담 | 플레이스홀더 우선 전략(§5.3). 시스템 완성과 비주얼을 분리 |
| 물량(50개 내외 스테이지)에 지침 | M4를 장 단위로 쪼개 "1장까지 플레이 가능" 같은 중간 완성 상태를 자주 만든다 |
| 맵 타일 단위 재현의 작업량 | M3.5 트레이싱 도구로 맵 1개당 작업 시간을 최소화. 도구 완성 전에는 맵 입력을 시작하지 않는다(수작업 낭비 방지) |
| 저작권 | 개인 플레이 전용, 원작 파일 미사용, 배포 없음 — 문제 소지 최소화. 단, 제작물 공개/배포는 하지 않는 것을 전제로 유지 |

---

## 10. 참고 자료

- 나무위키: 삼국지 영걸전 (게임 개요, 시스템)
- 더위키: 삼국지 영걸전/병과, /책략, /아이템 (수치 데이터)
- 나무위키: 삼국지 영걸전/선택지 (분기 조건 — M4에서 상세 수집 예정)
- 다음 카페 삼국지아카데미 등 공략 자료 (M4 데이터 입력 시 활용)

---

*이 문서는 v1.0이다. 구현 중 발견되는 원작 규칙의 세부 사항은 본 문서를 갱신하며 반영한다(§2, §3.3이 주 갱신 대상).*
